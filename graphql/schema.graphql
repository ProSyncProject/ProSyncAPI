type Query @guard {
    currentUser: User @auth
}

type Mutation @guard {
    logout: Boolean @auth
}

type Subscription @guard {
    userLoggedIn: User @auth
}

#import enum.graphql
#import model/*.graphql
